---
- hosts: winhost
  gather_facts: true

  tasks:
  - name: Run cacls from cmd
    win_shell: cacls c:\windows /t /c
    register: cacls_out

  - debug: debug cacls to screen
      msg: "{{cacls_out}}"
